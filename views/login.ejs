<%- include('partials/header') -%>
<%- include('partials/nav2') -%>

<style>
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .fade-in {
      animation: fadeIn 1s ease-in-out;
    }
  
    @keyframes animateWater {
      0% {
        background-position: 0 0;
      }
      100% {
        background-position: 100% 100%;
      }
    }
  </style>


    <div class="flex justify-center items-center mt-10">
        <div class="bg-black p-8 rounded-lg shadow-lg w-96 transform tansition duration-500 hover:scale-105 fade-in">
            <h2 class="text-3xl font-bold mb-6 text-white text-center"> Login To BlogoMania</h2>
            <form action="/users/login" method="post">

                <div class="mb-4">
                    <label for="username" class="block text-white text-sm font-bold mb-2">Username:</label>
                    <input type="text" name="username" id="username" class="shadow appearance-none border rounded w-full py-2 px-3 leading-tight focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-300">
                </div>
                
                <div class="mb-4">
                    <label for="password" class="block text-white text-sm font-bold mb-2">Password:</label>
                    <input type="password" name="password" id="password" class="shadow appearance-none border rounded w-full py-2 px-3 leading-tight focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-300">
                </div>

                <div class="flex items-center justify-between mb-4">
                    <button
                      type="submit"
                      class="text-white hover:text-black border border-white hover:bg-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline transition duration-300"
                    >
                      Login
                    </button>
            </form>
        </div>
    </div>
    






<%- include('partials/footer') -%>